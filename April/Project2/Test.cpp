#include <iostream>
#include <vector>
#include <queue>
#include <algorithm>
#include <climits>
#include <tuple>

using namespace std;

struct Person
{
	int x, y;
	int Ei, Ki;
	int id;
	int steps_to_exit;
	int exit_id;
};

struct Exit
{
	int x, y;
};

const int dx[] = { -1, 1, 0, 0 };
const int dy[] = { 0, 0, -1, 1 };

int main()
{
	return 0;
}